- Configuración inicial:
	- ```bash
	  git config
	  # configurar el user
	  git config --global user.name <nombre>
	  # configurar el mail
	  git config --global user.mail <name@mail.com>
	  
	  ```